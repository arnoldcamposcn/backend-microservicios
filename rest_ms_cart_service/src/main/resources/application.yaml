server:
  port: 8082

spring:
  application:
    name: rest-ms-cart-service

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info